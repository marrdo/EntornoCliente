"use strict";

////////////
//Functions
/////////////

//////////////
//Main
//////////////

let radios = document.querySelectorAll("");

let colors = ["verde", "marron", "negro", "amarillo", "rojo", "azul", "morado"];
let animals = ["Rodolfo", "Pepito", "Rocky", "Tote", "Gato"];

/**
 * Esto siguiente es para preguntas de la moodle, con estos botones se pueden eliminar.
 */
let divResultado;//BLABLABLA
let arr=[
    {id:1,nombre:"Javier"},
    {id:14,nombre: "Juan"},
    {id:123,nombre:"Paco"}
]
let s=";"
for(let o of array){
    s+=`<p><button id="btn-descartar-${o.id}">Info sobre ${o.nombre}</button></p>`;
};

divResultado = s;
